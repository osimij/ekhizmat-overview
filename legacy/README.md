# Archived pre-unification applications

These folders are historical references from before the unified four-route build. They are not Vite inputs, are not linked by the launcher, and must not receive product fixes.

- `Admin/` — former Admin prototype source.
- `Orgs/` — former Ministry/organization prototypes.
- `mvp2/` — former Citizen/Home project and operator references.
- `ЦОН/` — former standalone ЦОН project.

Use the active root routes and `apps/` code for all development. Git tag `pre-unified-2026-07-27` is the untouched recovery point.
